<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

<!--
    <ImageButton
        android:id="@+id/paginationArrowRight"
        android:layout_width="@dimen/paginationSize"
        android:layout_height="@dimen/paginationSize"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerPoster"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/paginationText"
        app:srcCompat="@drawable/chevron_right_circle" />

    <ImageButton
        android:id="@+id/paginationArrowLeft"
        android:layout_width="@dimen/paginationSize"
        android:layout_height="@dimen/paginationSize"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerPoster"
        app:layout_constraintEnd_toStartOf="@+id/paginationText"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/chevron_left_circle" />

    <TextView
        android:id="@+id/paginationText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="-/-"
        android:textSize="@dimen/paginationSize"
        android:tint="@color/colorTextGray"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerPoster"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
-->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerPoster"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="64dp"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>